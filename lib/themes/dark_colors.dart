import 'package:flutter/material.dart';

class DarkColors {
  static const primary = Color(0xFF121212); // AppBar background in dark
  static const accent = Colors.orange; // buttons / highlights

  static const background = Color(0xFF121212); // scaffold background
  static const card = Color(0xFF1E1E1E);
  static const container = Color(0xFF1C1C1C);
  static const divider = Color(0xFF424242);

  static const textPrimary = Colors.white; // main text
  static const textSecondary = Colors.grey; // secondary text
  static const textHint = Color(0xFFBDBDBD);

  static const iconPrimary = Colors.white; // AppBar icons
  static const iconSecondary = Colors.grey;

  static const badgeBackground = Color(0xFF880E4F);
}


import 'package:flutter/material.dart';

class LightColors {
  static const primary = Colors.white; // AppBar background in light
  static const accent = Colors.orange; // buttons / highlights

  static const background = Colors.white; // scaffold background
  static const card = Color(0xFFFDF5F5); // card background
  static const container = Colors.white;
  static const divider = Color(0xFFBDBDBD);

  static const textPrimary = Colors.black; // main text (titles)
  static const textSecondary = Colors.grey; // secondary text
  static const textHint = Colors.grey;

  static const iconPrimary = Colors.black; // AppBar icons, active icons
  static const iconSecondary = Colors.grey; // inactive icons

  static const badgeBackground = Color(0xFFFCE4EC);
}

